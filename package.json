{
    "name": "megera-root",
    "private": true,
    "workspaces": [
        "lambda",
        "testOAIkey"
    ],
    "type": "module",
    "scripts": {
        "key:test": "node testOAIkey/testOpenAIKey.js",
        "lambda:lint": "echo 'Nenhum lint configurado (ESLint recomendado)'",
        "lambda:deploy": "cd lambda && npm install --omit=dev && zip -r ../function.zip . -x 'node_modules/aws-sdk/*' '*.md' 'test*/**' && aws lambda update-function-code --function-name megera-chatgpt --zip-file fileb://../function.zip ${AWS_PROFILE:+--profile ${AWS_PROFILE}}",
        "deploy": "npm run lambda:deploy"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
